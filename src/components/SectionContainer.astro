---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`w-full pb-52 scroll-m-20 mx-auto container lg:max-w-4xl md:max-w-2xl ${className}`}
>
  <slot />
</section>
